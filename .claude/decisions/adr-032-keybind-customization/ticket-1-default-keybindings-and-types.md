---
title: Define default keybindings registry and types
status: done
priority: critical
assignee: sonnet
blocked_by: []
---

# Define default keybindings registry and types

Create the foundational types and default keybinding map that all other tickets depend on.

## Files to touch
- `src/lib/keybindings.ts` — new file, all types and defaults
