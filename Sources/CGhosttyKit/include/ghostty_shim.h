#ifndef GHOSTTY_SHIM_H
#define GHOSTTY_SHIM_H

// Local copy of ghostty.h — avoids the xcframework's module.modulemap
// which conflicts with SPM's module system. Sync with:
//   make sync-ghostty-header
#include "ghostty.h"

#endif
